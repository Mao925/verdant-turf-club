# 馬主ゲーム実装の設計

製品設計の正本：Vault `スポーツ経営ゲーム/馬主ゲーム/2026-09-08_馬主ゲーム設計書.md` v0.4。採用判断はD01〜D13。Vite＋React＋TypeScript、端末計算、Supabase Free、Google OAuth（PKCE）、保存確認まで次の変更を止める。

## 責務と保存契約

- domain：安定したUUID、日単位の暦、整数円の台帳、馬・所有・契約。純粋な関数で変更を計算。
- engine：Web Workerへ命令を渡す。Reactや保存SDKに依存しない。
- application：確定状態、命令ID、IndexedDBの未送信データ、クラウド応答、競合を管理。未送信を消さず同じ処理を再送。
- persistence：Supabaseの保存RPCとRLS読込み。IndexedDBはアカウント別の復旧領域。
- UI：ログイン、馬主ホーム、台帳、保存状況、再試行・クラウド最新状態の読込み、書出し・取込。Three.jsの毎フレーム更新は独立させる。

ゲーム用テーブルは `owner_saves`（利用者ごとの1つの進行中セーブ・版・小さいコア状態）、`owner_entities`（馬・契約・台帳・出来事）、`owner_commits`（命令ID・要求ハッシュ・確定版）、`owner_checkpoints`（復旧用の直近3保存版）。命令RPCは利用者とsaveIdの所有者を検査し、行ロック後に同じ命令の再送を確認し、expectedRevision一致時だけコアと変更エンティティ・命令IDを一括確定する。同じIDで異なる内容を送る要求はSHA-256による要求照合で拒否する。通常のRPC応答は版番号だけにし、端末に保護した計算結果を確定する。再送確認時に他端末の進行が先行していれば、最新の状態を別途読み込む。

通常の保存は変更したエンティティだけ送る。P1では一頭の状態と小さいコアを扱い、過去の全台帳を毎回複製しない。復旧用チェックポイントは最大3版、一定間隔で作り、容量を計測する。過去のイベントと台帳は現在のセーブのエンティティとして保持する。入力サイズ上限は実装上の検証用制約で、正式版の長期容量の保証ではない。

## 失敗時

保存前に命令と計算結果をIndexedDBへ保護。保存確認までUIの変更をロック。応答消失は同じ命令IDの再送で確認。他端末の先行保存は競合として表示し、未送信をファイルへ残して最新を読み直す。結果・台帳を自動合成しない。ログアウト後も前の利用者のデータを別利用者の画面へ渡さない。取込はスキーマ・参照・金額・日付を検証し、現状態を保護して明示した復旧命令として保存する。確定前の未送信も書き出せるが、クラウド確定版とは区別する。

## セキュリティと検証

RLSで本人のみSELECT。直接DMLは禁止し、SECURITY DEFINERの関数内でauth.uid・所有者を検査、空search_path・完全修飾名・限定EXECUTEを使う。クライアント計算の改変防止を保証しない。型検査、命令と台帳のテスト、通信失敗・応答消失・競合のテスト、実PostgreSQLの権限/原子性検証、ブラウザのIndexedDB・画面検証を分離する。Google OAuthと実クラウド接続は設定完了後に検証する。

## 開発クラウドへの接続

Supabase CLIの接続キャッシュは `supabase/.temp/` へ置き、Gitから除外する。DB定義と適用履歴は `supabase/migrations/` の版番号で一致させる。初回の適用後、再度のdry-runで未適用がないことを確認する。

現在の開発先は専用Freeプロジェクト。アプリには `.env.local` の公開URL・publishable keyだけを渡す。管理用トークンとDBパスワードはMacのキーチェーンで扱う。Authの戻り先は `http://localhost:5173/` と `http://127.0.0.1:5173/`。Googleの戻り先には実Supabaseの `/auth/v1/callback` を登録する。P2配信時に固定URLを追加し、ワイルドカードで任意の配信先を許可しない。

実クラウドの初回SQL検証は空の専用プロジェクトで一つのトランザクションを使い、最後にROLLBACKする。auth.uidの入力にはテスト用UUIDを使うため、Googleログインの検証とは区別する。

## P2の実装契約（2026-09-09）

- 保存版をowner-p2 / prototype-2026へ更新。P1は読込み・書出しを維持し、移行命令で過去の個体・残高・イベントを残して補足状態を作る。未送信P1命令も旧ルールのまま再送してから移行する。未知の版は拒否する。
- 市場の候補とNPC入札は世界シード・開催回ごとに固定する。落札不成立を記録し、再表示で評価や相手上限を引き直さない。一頭の取得と債務確定、支払・引渡し、預託を別の処理にする。
- 契約は月末締め翌月7日支払、日割りの未請求額を保持。請求書は支払済み台帳と分け、未払い時は暦を止める。賞金ゼロ12か月予測は実際と同じ日割り・請求日・1月拠出を使う。生活終了/試作終了も債務や履歴を削除しない。
- 馬は非公開の基礎能力・適性・疲労を保持し、UIは観察を表示。相談は担当者固有の文面、直近合意、実際の結果・変更理由だけから生成。休養は負荷と機会を変え、目標を変えても速度に直接加算しない。
- 日送りは次の相談、登録締切、選出、競走、請求、年末振返りで停止。登録・選出・取消・結果・精算は別状態。出走資格は年齢/クラス/登録/ゲート/在厩日数/回復を照合する。P2は2歳から3歳8月末まで、未収録路線へ進む際は記録を残して試作終了を明示。
- 競走はWorkerで一度だけ計算し、安定した馬IDの能力を使う。結果と再生用の小さい経過を保存。3Dは描画枠番号との対応表を持ち、距離を既存トラックのワールド座標へ正規化する。再生速度・スキップ・端末種別は計算・精算に影響しない。描画は既存コース造形を共用する簡略表現。
- 検証は購入失敗/預託/期限停止/取消/競走再現性/二重精算/債務保持/移行/破損拒否、既存の保存障害、PCと390pxの全判断、実DBと実Googleでの配信再開に分ける。単独の成功でF01〜F12完了としない。
- 配信は既存Vercelを再利用。ソースの秘密情報を除外し、公開用Supabase設定だけをビルドに渡す。DBの復旧用エクスポートはVault/Git外へ暗号化して保存する。配信前のバックアップと復元検証、固定URLでのAuth往復を確認する。

### P2実サービス検証で見つかった競合通知の修正

古いexpectedRevisionに対してSQLSTATE 40001をアプリ都合で発生させていたが、実Supabase HTTP経由では応答が25秒以上戻らないことを検出した。直接のPostgreSQL検証とモックだけでは検出できなかった。40001はHTTP 500になるため、業務上の版競合にはPT409を使い、HTTP 409として即時に通知する。クライアントは旧40001も読み取れるようにする。ゲートウェイ内部の再試行が原因かは未観測。修正後は同じ古い版のHTTP呼出しが約43msで409/PT409を返し、実Auth・実RPCのブラウザ競合と復旧まで成功した。外側のAPI応答と修正後の結果を検証証拠とする。[PostgREST公式のエラー対応](https://docs.postgrest.org/en/v14/references/errors.html)。

在厩日数は初出走15日・既走10日、発走審査の通過を照合する。[JRA馬主FAQ](https://www.jra.go.jp/owner/members/faq/category_d.html)。14日の出走間隔、毎週の東京芝1600mと中山ダート1800m、締切7日前・選出3日前、除外の発生、賞金・費用・能力係数はP2の比較検証用の仮定。実際の開催日程・賞金の完全再現はしていない。P3の通年番組で再設計する。

## P3の実装契約

- owner-p3 / calendar-2026を追加し、旧版の読込み・未送信再送を維持。明示的な移行で暦・所有ID・台帳・契約・未精算を残す。P2の収録期間終了のみ解除し、自発的な活動終了は保持。
- 通年番組は年と規則から生成し、未実施の全競走を保存しない。実施した競走は安定IDで記録。背景競走は着順・時計の小さい記録、所有馬の競走は再生経過を保持する。
- 各馬の路線・目標・次回相談を個別に保存。現在の選択馬は既存UIへ投影し、日送りは全頭の相談・選出・競走・請求を一緒に処理する。複数馬が同一競走へ申込可能。未決の判断があれば全体の暦を止める。
- NPCは永続する馬主・個体・収得賞金・戦績を持ち、プレイヤー不参加の番組にも自律的に申込む。馬齢と路線・回復・収得賞金で候補を絞る。定員超過は優先権・実績・同順位の固定抽選で選出し、理由を保存する。
- 5場の方向・直線・曲線・高低差を独立したコース定義にし、計算に反映。3Dは簡略造形を維持し、馬番と永続ID、複数所有の追跡を分離する。
- 収得賞金と実収入を分離。預託の合算予測は各契約の日割りを合計し、既存契約と新規取得の追加負担を比較する。資金帯は3,000万/600万、1億/2,000万、5億/8,000万の設定を検証する。
- 保存RPCの許容版を追加。命令履歴は再送の安全性を保ちつつ要求全文の重複を避ける。実際の1年分の保存・DB容量・転送・日/月送りを計測して無料枠を見積もる。
- 検証：旧段階全状態の移行、複数所有の個別相談・同日/同一競走・二重精算、資格と選出の境界、全路線の暦、NPCの継続性、契約予測と実績、1年の世界負荷、PC/スマートフォン、実SupabaseとVercel。

### P3の保存・規模検証で具体化した点

競走条件と賞金の内訳はJSONBのキー順序に依存せず値で照合する。キー順を反転させる回帰テストと実Supabaseの再読込みで確認する。未知の条件・余分なキー・賞金の改変は引き続き拒否する。

年間1,036競走（重賞40）を生成し、3歳春の1勝/OPも全路線へ用意。年末の選出では翌年1月の番組も対象にする。NPC落選者全員の文字列・過去の全再生経過の重複保存を避け、背景競走は着順と時計、所有馬出走は再生と個別賞金を保持する。NPCの回復は前走からの経過日で算出し、日々の全頭更新を避ける。

一方、Workerへの受渡しと検証では現在の世界を構造化コピーする。4頭/初期NPC2,040頭の一年でコピーp95約16.9ms、保存3.93MBを測定。任意の年数での容量・速度保証とはしない。P4以降の引退/死亡/繁殖と世代交代を加え、数年・親子2世代で再測定し、履歴の読込み単位を再評価する。

## P4の実装契約（2026-09-09）

- owner-p4 / life-2026を追加。P1/P2/P3の保存・未送信再送を維持し、移行以前の健康を遡って抽選しない。馬の生命、競走資格、所在地、所有を独立して持つ。人物場面と診療/移動/売却のエピソードを参照IDで記録する。
- 日送りと競走確定にP4の処理を接続する。健康の乱数は世界・馬・日付・活動から固定し、描画/再表示/支払で引き直さない。一原因一エピソード。競走中止は順位・賞金と区別し、描画は保存済み中止地点で止まる。競走後に判明する異常も別経路で扱う。
- 初期対応は事前合意した委託契約に基づき自動実施、必要費用は債務にもなる。非致死的な診療では医学上の選択肢、固定した再評価日、費用、見通しと不明点を示す。支払だけで治癒しない。追加所見は同じエピソードを読む。致命的外傷の生命予後を安い維持費へ誘導する選択肢にしない。
- 契約切替の精算は当日を旧契約へ二重請求せず、終了日より前の未請求分を請求へ振り替える。移動中の費用は新契約が負担し、到着してから在厩日数を数える。引退預託も所有と月額費用を維持。死亡時は新しい請求/出走を止め、既発生分を精算して未払を残す。
- 売却は照会、需要に応じた提示/不成立、合意、7日後の引渡しと入金を分ける。健康急変は手続きを取消し、将来の希望価格を今日の現金にしない。元所有馬はNPC世界で継続する。牧場の受入枠と健康条件を確認し、受入先不在を正規の状態にする。
- 財務は合意のある一回の14日猶予を請求ごとに記録。新しい拠出枠や債務免除を生まない。継続できなければ現在の現金・確定債務・未解決の飼養先を記録して活動終了する。残った馬の生涯保証と表示しない。
- 人物は担当者固有の文章と、当時の相談/診療/支払/競走IDに基づく記憶を持つ。転厩、再会、療養の判断、死別、年末に場面を作る。一言を残せるが、好感度を馬の速度へ加算しない。プレイ中の外部AIは使わない。
- NPCにも同じ原因別健康判定と競走停止を適用し、一定の馬齢でNPCの競走登録を終了する。プレイヤーの自動引退はしない。履歴は保持し、3年進行・実保存・DB容量でP3からの増分を測定する。

### P4の実保存で見つかった処理時間制限

3年分9.84MBの復旧を実Supabaseで試したところ、逐次INSERT版ではHTTP 500・PostgreSQL 57014（statement_timeout）となった。通常の診療・移動・再開は成功。202609090005でエンティティを一括検査・一括UPSERTへ変更し、1命令1トランザクション、CAS、内容ハッシュ、旧版の再送、3件の復旧履歴を維持する。一括化後の再検証と追加対応は下記のとおり。P3の原因未特定の一時失敗とは区別し、今回のコードが前回の原因も証明したとはしない。

一括化後も大きなRPCで57014が再発したため、認証ロール既定8秒を確認した。公式の関数別設定に従い、保存・読込の2RPCだけ30秒、ブラウザの通信は転送を含め45秒へ変更した。他の認証・匿名ロールの制限は変更しない。タイムアウト時は既存の未送信保護と同じ命令の再確認を使い、確定前の日付操作を止める。[Supabaseの関数別タイムアウト](https://supabase.com/docs/guides/database/postgres/timeouts)。

再検証は3年世界9,787,806B・10,355エンティティで4回成功。復旧17.73/25.79/33.17/27.81秒、読込15.73秒、ブラウザ再開16.08秒を観測した。通常操作/障害/診療の実RPCも成功。30秒はDB実行枠であり、HTTP合計時間とは異なる。さらに長い経歴の転送分割・履歴分割と人数の上限はP5/P6で検証する。


## P5の実装契約（2026-09-09）

- owner-p5 / breeding-2026、繁殖エピソードと個体の両親/育成/繁殖所見を追加する。両親は永続個体を参照し、旧馬の架空血統文字列から過去の個体を捏造しない。母が生存して引退していることと、繁殖に適することを別に検査する。
- 外部牧場は白樺牧場を継続。繁殖預託と育成の目的を既存契約へ追加し、母と将来の仔の枠を確保する。所有12頭・牧場6枠の上限に出生予約を含め、死亡・不成立・辞退で予約を解放する。競走・移動・売却・妊娠が同時に成立しない。
- 申込み→受入提示→予約→種付け→受胎報告→妊娠の継続→分娩→生後31日を暦の別時点で処理。繁殖期2月10日〜6月30日、妊娠330〜350日を仮設定とする。種付け日と父母のIDから転帰を固定し、再読・追加支払・種牡馬変更で既成立妊娠を引き直さない。
- 受胎後支払は9月30日の受胎確認時に請求を確定し10月31日を支払期日にする。出生後支払は生後31日に発生し翌種付年10月31日が期日。30日以内の仔の喪失等では種付料のみ免除/返還。提出書類は預託先が適時処理する架空契約。遅延損害金等の実規程との差は明記。未払いの取消と支払済みの返還を台帳で区別し、母仔の実費を消さない。
- 不受胎、早期/後期の妊娠喪失、死産、新生仔死亡、母死亡を分ける。出生前は妊娠記録、死産以降は仔の個体と死亡記録を保つ。種付け後の父死亡で妊娠を消さず、出生後の母死亡では仔の飼養を継続する。活動終了時は未解決の妊娠/飼養/債務も残す。
- 仔は出生→180日後離乳→1歳→育成開始（1歳9月以降）→最低180日と2歳4月以降の準備完了→入厩/審査を経る。育成前の出走を拒否。能力は両親の傾向＋個体差と成長段階で決まり、種付料や親の最大値を足して強さを保証しない。母の目標を継ぐ/変える理由と仔自身の競走を記憶へ接続する。
- 1歳市場は既存の入札/取得を再利用し、引渡し後は育成預託へ進む。2歳市場の旧途中状態は維持する。将来の種付料を負債確定と混同せず、費用予測に条件付き額と母仔別の期間を示す。
- 全P1〜P4読込・旧未送信再送を維持する後方互換SQLを追加。親子2世代の長期セーブで既存20MB要求上限/30秒RPC/45秒端末枠を検証し、必要な分割や容量対策を実測に基づき追加する。バックアップ/隔離復元後に実DBを更新し、候補資産一致→既存Vercel昇格→公開実RPC確認の順で反映する。

P5の仮条件として、診察は7日後に所見（個体固定の受入不可6%）、申込回答は3日後、種付けまで最低7日。種牡馬側の受入不可8%は年度・父で固定し、受付枠は4/3/2件。母は3〜18歳（予定交配年の年齢も確認）、産後は180日、流産後は60日の休養を取るため、多くは毎年繁殖できない。現実の発情周期や産後初回発情管理は再現しない。生後30日は繁殖の新生仔モデルを用い、成馬の日々の一般ハザードを二重に適用しない。通常診療の母死亡でも妊娠/仔の契約を照合する。

繁殖相談の費用比較は、申込3日と予約後7日を含む予定日から出生340日後を見込み、現在から仔の2歳6月までの母仔の飼養・育成、種付実施10万円・分娩30万円・条件付き種付料を年平均日割りで概算する。既に支払った診察/移動は再加算しない。将来の輸送/追加診療/難産/再交配と入厩後費用は別。実際の月日割りと未来の全状況を予測するものではない。

通し検証で、選出から除外された母馬を引退させると過去の登録だけを削除し、除外との参照整合性が壊れる問題を発見した。除外済みの選出記録を変更しないよう予定取消を修正し、引退を通す回帰ケースを追加した。

### P5の大容量送信への追加対応

13.9MB世界の初回復旧は成功したが、2回目はREST520/HTML、再試験は45秒超過で失敗した。DBのOOM/再起動/statement_timeoutは記録0、426MB物理メモリと約213MBのswap使用を観測。512MiBの隔離PostgreSQLでは全4回が成功（p95約6.43秒）し、クラウド経路のボトルネックを断定できない。待機時間の増加だけを続けず、4MB以上の要求は1MB単位で非公開の一時表へ送信し、最後のRPCで既存commitのCAS・内容ハッシュ・チェックポイント・一括確定を使う。

一時表はログイン利用者ごとに最大1アップロード、最大32チャンク/合計20MB。RLSを有効にし直接操作権限は与えず、各RPCでauth.uidを検査。開始/同一チャンク再送の内容照合、欠落拒否、完了後の一時データ削除を行う。別の復旧が開始された場合も本セーブを部分更新しない。再読時の未送信保護を継続し、応答消失後は同じ全体命令IDで再構築・再確認する。一時表は確定セーブのバックアップ対象から除外し、認証ユーザー削除では一緒に削除する。既存P1〜P4クライアントの6引数commit RPCは維持する。

分割送信後は最終RPCのHTTP500/57014を約30.24秒で捕捉した。最初の保存は約14.71秒、最終RPC約4.12秒で成功したが、既存13.9MBの保管を伴う二度目が遅い。JSONBの大きな集約・入れ子コピーを減らし、JSONテキスト集約から最終JSONBへ一度変換する。復旧時は対象から消えたIDだけを削除し、内容不変のエンティティを再書込しない。復旧の内容ハッシュ・CAS・毎回の旧版保管・版番号は維持し、別内容の復旧も検証する。
